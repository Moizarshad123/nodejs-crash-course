//settimeout runs one time after 3 seconds
// setTimeout(() => {
//     console.log("in the timeout");
//     clearInterval(int);
// }, 3000);


// setInterval runs continue after 1 second
// const int = setInterval(() => {
//     console.log('in the interval');
// }, 1000);

console.log(__dirname); // get the absolute path on the current folder
console.log(__filename); // get the absolute path on the current folder with the current filename
